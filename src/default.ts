// defaults.ts

import { IconBarSettings } from 'types'

// Default plugin settings
export const DEFAULT_SETTINGS: IconBarSettings = {
	logs: {}, // Initialize logs as an empty array
	compatibility: '1.0'
};
